const {%ComponentName%} = (state = {}, action) => {
  switch (action.type) {
    case '{%ComponentName%}':
      return Object.assign({}, state, action.data);
    default: {
      return state;
    }
  }
};

export default {
  {%ComponentName%},
};
